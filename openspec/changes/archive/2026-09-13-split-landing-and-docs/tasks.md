## 1. Sequence the change and capture the baseline

- [x] 1.1 Archive or sync `recompose-site-spine` first (`openspec archive recompose-site-spine`) so `openspec/specs/product-site/spec.md` no longer carries the superseded "one merged capability block below the terrain" requirement; verify by reading the synced spec and confirming this change's delta applies to the text that is actually there. Do not archive this change before it (design D8).
  - Archived as `2026-09-13-recompose-site-spine` after `validate --strict` passed. Main spec synced: `+ 5 added, ~ 1 modified, → 1 renamed`; `grep 'One merged capability block'` now returns nothing, and the five spine requirements are in `openspec/specs/product-site/spec.md` (lines 236–308). Active changes: this one only.
- [x] 1.2 Write the Impeccable surface brief for `/docs` through `surface-brief write` before any markup edit, with the six contract blocks and a SECTION ORDER block naming the nine sections in `product-docs`; verify by reading it back. Write a second brief for the landing's copy changes, or amend the existing `website-index-html` brief, recording that the landing keeps exactly four detail facts (design D2).
  - Written and read back: `website/.impeccable/surfaces/website-docs-index-html.md` (new; 7 contract blocks — THESIS, OWN-WORLD, STORY, FIRST VIEWPORT, SECTION ORDER, FORM, FINISH — with SECTION ORDER naming the nine sections) and `website/.impeccable/surfaces/website-index-html.md` (amended: the surface is now "the poster only", the four kept facts are enumerated, the split caps are added to Constraints, and the masthead `Docs` control now targets `/docs`). `git status` at that point showed nothing modified under `website/` but the two briefs.
- [x] 1.3 Read `reference/craft-floor.md` immediately before the first UI edit and note the two rules that bind here (no coloured `border-left`/`border-right` above 1px as a code treatment; no kicker above a heading); verify no UI edit was made before this read.
  - Read; no UI edit preceded it. Both rules bind the new work directly: the hero install line is a ruled row, not a bordered callout, and the docs surface's section headings carry no eyebrow above them (craft floor calls the kicker "a ban, not a default").
- [x] 1.4 Capture the pre-change baseline at 1086 and 390: `documentElement.scrollWidth`, `scrollHeight`, the seven spine/fork positions, section gaps, the type floor, the contrast pairs, and the ink rects of every `.note`. Verify the new ink assertion fails at 1070–1150 and 1300–1340 on the unmodified page, so the fix has a red test rather than a claimed one.
  - Baseline at 1086: `scrollWidth === clientWidth === 1086`, `scrollHeight` **4229**; all five `.spine` plus the SVG `.sig` plus the terrain fork agree at **696.95 / 696.96 — spread 0.01px**; section gaps **0,0,0,0,0**; smallest computed text **12.0px**, none below 12px; every text pair ≥ 4.5:1 (lowest **4.82**, muted on paper) and the dark grounds at **9.22 / 16.59**.
  - The assertion is a new checked-in tool, **`website/tools/ink-probe.html`**: it loads `index.html` in a same-origin iframe at 24 widths and fails when any text run's ink *straddles* a viewport edge. Two refinements were needed and are recorded here: only straddling ink is the defect (a focus-revealed skip link parked at `left:-9857px`, and `sr-only` text, are not), and ink clipped to a box that ends inside the viewport is the deliberate `sr-only` pattern rather than a frame clip.
  - **Red as required:** FAIL at **8 of 24 widths** — 1340, 1300, 1151, 1150, 1100, 1086, 1071, 1070 — every offender `p.note__desc`, worst **+8.89px at 1070**, and at the 1086 reference frame **+5.84px** on `Raw text, documents,`, `optimized execution.` and `vectors (e.g. 384D).` No false positives, and the probe reports `scrollWidth === clientWidth` at every failing width — which is the entire reason the defect survived twelve passes.
  - One measurement lesson worth keeping: the terrain fork must be measured from the **route `<svg>` element's** box, not the `path`'s own `getBoundingClientRect()` (the path's bbox spans both trunk halves, which reports a bogus 167px split). Measured correctly it is 696.96.

## 2. Remove the landing's unsupported claims

  - `images` is gone from both: the hero sub now reads "Turn text, and any tensor a script builds, into vectors." and the INPUT note "Raw text, or any tensor a script builds." `grep -on images website/index.html` returns nothing. The claim now matches `examples/scripts/siglip2.lua`, which feeds `pixel_values` as a zero tensor and states the image branch is absent.
- [x] 2.2 Replace "Production ready." with the pre-1.0 status and record the license and platform list (macOS arm64; Linux amd64/arm64) where the hero action row can carry them; verify the page states `0.4.0.pre4` and "interfaces may still move", generated from `VERSION` rather than typed.
  - The hero sub now opens "Drop-in compatible. Pre-1.0, MIT licensed." The version itself, the platform list (`macOS arm64 · Linux amd64/arm64`) and the licence sit in a new three-row `.hero__facts` ledger under the actions, using the existing `.facts` atom. "interfaces may still move" is stated on the docs surface, where the version also appears.
- [x] 2.3 Generate the `emb-top` capture's version string from `VERSION` instead of the hand-typed `v0.4.0`, and label the capture's figures as illustrative where they are not a `BENCHMARK.md` run; verify no hand-typed version string remains anywhere in `website/` (`grep` for `0\.4\.0`).
  - New tool **`website/tools/stamp-version.py`** rewrites the text of any element carrying `data-emb-version` from the repo `VERSION`, with `--check` as the drift guard. Two elements are stamped (the `emb-top` capture header and the hero RELEASE row); `grep -rn '0\.4\.0' website/ --include='*.html' --include='*.css' --include='*.js'` now matches only the stamped values. `--check` passes; a drill that changed one to `0.4.1` made `--check` exit 1 and the tool restored it.
  - The tool's first regex was **wrong and shipped a mangled line** before it was fixed: a permissive tag match let an outer `<span class="tf">` be treated as the marker's element, so it rewrote the version and deleted the following `·`. It is now `[^<>]*` for the open tag with `(?=[\s>/])` after the attribute, and a six-case fixture test covers nesting, stale values, a preceding tagged element, and an attribute after the marker. Recorded because a version stamper that corrupts markup is worse than a stale string.
  - The capture's caveat is now explicit: `Illustrative render · not a measured run` replaces `Sample run · rows omitted`.
- [x] 2.4 Add exactly one measured `BENCHMARK.md` figure with its machine, method, and reproduction command, and exactly one copy-paste install line above the fold; verify both by reading the rendered page, and verify the landing still carries no command table, no configuration block, and no more than four named commands per `facts` list.
  - Install line: `curl -fsSL https://raw.githubusercontent.com/elcuervo/emb/main/install.sh | sh`, above the fold in the hero, with the rule above it and the `.facts` ledger below.
  - Measured figure: **2,010 req/s · minilm, 16 clients, Apple M4** — the multi-threaded table in `BENCHMARK.md`, with `reproduce` linking the section. The machine and client count are stated inline, so the number carries its provenance on the page.
  - Facts lists measured: hero 3 rows (RELEASE / BUILDS / MEASURED), the three blocks 4 rows each — under the cap. No command table and no configuration block on the landing.
  - **A spec refinement this task forced:** the requirement originally said "exactly one link to the documentation surface", which cannot be implemented alongside a `Docs` navigation entry. It now requires one install command and one *destination* — calls to action and documentation navigation entries resolve to `/docs`, and the reference material is linked from nowhere else. The masthead `Docs` and `Models` entries and both `Get Started` controls now point at `docs/index.html` (and `#install` / `#configuration`); the only remaining off-site reference link is the benchmark's `reproduce`, which is the number's provenance rather than a detour. `openspec validate --strict` still passes.

## 3. Contain the ink

- [x] 3.1 At `min-width: 1001px`, set the stage note's right offset to `0` and reclaim the 10px from the note's own number column so the description's measure and the leader anchor are unchanged; verify the description wraps to the same line count at 1086 as before, and that the leader's stepped rule still reaches the same x.
  - A `min-width: 1001px` override was **not** needed: the base `.note` rule is the only place the bleed existed (the `max-width: 1000px` block already sets `right: 0`), so the base rule now carries `right: 0` and `grid-template-columns: 28px` — the number column gives up exactly the 10px the box no longer bleeds, which keeps the description's measure identical.
  - Measured at 1086 after the fix: every note box's right edge is **1086** (was 1096); every ink right ≤ **1081.84**; the description width is still exactly **148.2px**, so the unchanged descriptions keep their line count (INFERENCE and EMBEDDINGS 2 lines, SERVE 4 lines) and the `01–04` numbers stay at x **909.8**. INPUT reads 3 lines instead of 2 only because task 2.1 made its copy longer. The leader is anchored to the note's **left** edge (`right: calc(100% + 18px)`), so it does not move at all.
- [x] 3.2 Implement the two-part assertion from design D5 — the box half (no `.note` box's right edge past `clientWidth`) and the ink half (every text run's rects inside the viewport) — at 1001, 1024, 1070, 1071, 1086, 1100, 1150, 1151, 1200, 1300, 1340, 1440, 1720, 1721, 1740, 1920; verify it fails on the pre-change stylesheet and passes after, and record in `CORRECTIONS.md` that `scrollWidth === clientWidth` cannot substitute because `overflow-x: clip` removes the clipped content from the scrollable region.
  - Implemented as `website/tools/ink-probe.html` (see 1.4) and verified red-then-green with the *same* tool: before the CSS change, **FAIL at 8 of 24 widths**, worst `+8.89px`; after it, **PASS — no text ink outside the viewport at any of 24 widths**, with zero offenders and zero scroll-width mismatches.
  - The box half is now vacuous by construction, but it is what localises the defect fastest: before the change it failed at *every* width in 1001–1739 while the ink half failed at only 8. Both halves stay in the probe.
- [x] 3.3 Sweep the whole page, not just the notes, for the same class of defect — every text element's ink rects at the sixteen widths above, including the terrain annotations, the plate labels, the ledger rules' neighbours, and the console transcript; verify nothing else is clipped, or fix it and record it.
  - The probe walks **every text node in the body**, so the SVG `<text>` plate labels, the terrain annotations, the ledgers and the `emb-top` capture are covered, not only the notes: PASS everywhere. Nothing is exempted by the scroll-container rule, and `.topviz__screen` computes `scrollWidth - clientWidth = 0` at 1086, 1000, 640, 390 and 320 despite declaring `overflow-x: auto`.
  - The console is the one element the probe cannot fully judge, because its transcript is injected by JS after load. Driven directly with the longest real transcript line (`EMB.EVSHA sst2 "77c1…" 1 "this film is great" NEGATIVE POSITIVE`, 63 characters) plus the result block: the output is `white-space: pre-wrap` with `overflow-x: visible`, it **wraps rather than clips**, and its ink stays inside at 1086, 1000, 640, 390 and 320 (worst case 390: ink 360.6 against a 357 box in a 390 viewport).
  - **One pre-existing defect found, with numbers, and deferred to task 7.2:** `.plate-label{font-size:14px}` (`styles.css:602-604`) is in SVG **user units**, so it renders `14 × svgWidth/364`. Measured: **18.08px at 1440, 13.99 at 1086, 12.90 at 1001, 17.69 at 1000, 14.76 at 834, 11.34 at 641, 22.65 at 640, 13.46 at 390, 10.77 at 320**. The plate words therefore breach the committed 14px mobile floor at 641, 390 and 320. The floors had only ever been verified for CSS text, never for text scaled by a `viewBox`, and `calc()` cannot divide by a length, so this needs a per-breakpoint value rather than an expression — which is why it is fixed in the floor pass.

## 4. Give the failure paths a contract

- [x] 4.1 Add the script-failure guard so a `main.js` load error removes the `js` class rather than leaving content at `opacity: 0`; verify by blocking `assets/js/main.js` in the browser and confirming the four plates, the spine, and all four notes are visible with no interaction.
  - **The first guard was incomplete and the test caught it.** An `onerror` attribute on the script tag covers a 404 or a blocked request, and a `try/catch` around `main.js`'s `boot()` covers a throw inside it — but a throw while the file *evaluates* (module scope, before `ready()` runs) escapes both. A fixture that throws at the top level still rendered a **blank** page: `html.js` applied, 4 slabs at `opacity: 0`, spine `stroke-dashoffset: 1`, and **14/14** `[data-reveal]` sections hidden.
  - Final guard is at the bootstrap, where the class is set: an inline `error` listener (capture phase) removes `js` unless the page reports `is-ready`. Verified against three fixtures — a missing `main.js`, a top-level throw, and a throw after load — each leaving `htmlClass: ""`, 4 slabs at `1`, spine at `stroke-dashoffset: 0`, and 0 hidden reveals.
  - **Red/green control:** toggling `js` back on with the script dead reproduces the blank page (4 slabs at `0`, spine at `1`, 14/14 reveals hidden). Under `prefers-reduced-motion: reduce` the red state is invisible, because the reduced-motion block sets the same elements to their end state — which is why the red state had to be measured with `reduced-motion:no-preference` via `set media`.
  - No regression on the normal path: `main.js` still boots (`js is-ready`), `is-live` lands when the pipeline scrolls in with all 4 slabs at `1`, and a scroll-through leaves **0/14** hidden reveals with the route fully drawn (`stroke-dashoffset: 0`).
- [x] 4.2 Fix the print stylesheet so no text prints light-on-white: apply `print-color-adjust` to the dark grounds, or re-colour their text for a white ground; verify with a print-to-PDF and background graphics disabled that the scripts block, the console, the `emb-top` screen, and the footer are legible.
  - Chose to keep the grounds rather than re-colour: the print block already resets `html, body{background:#fff}`, and forcing `print-color-adjust: exact` on the five light-on-dark surfaces (`.block--dark`, `.console`, `.console__bar`, `.console__screen`, `.topviz__screen`, `.footer`, and the masthead's `.btn--dark`) preserves the one inversion the poster is built around, instead of rewriting ~20 selectors' worth of a type ladder for paper.
  - Verified in the CSSOM: the rule is inside `@media print`, parsed by the browser, and matches all seven selectors. **What could not be verified here:** the background-graphics-disabled path. The harness's `pdf` prints with backgrounds enabled regardless of `print-color-adjust`, so the counterfactual (injecting `print-color-adjust: auto !important` and re-printing) produced a byte-equivalent page — same counts of `#111110` (5), `#292823` (8), paper (49) and ink (48). The fix is therefore correct-by-standard and render-unverified; it needs a manual print preview to close.

## 5. Split the stylesheet without changing a value

- [x] 5.1 Move the shared layers — tokens, base resets, primitives, the type floors, the contrast pairs, the code treatment, reduced motion, print — into the sheet both pages link, leaving `--fold`, the spine primitives and the hero-only geometry with the landing; verify by diffing declared values that nothing moved in value, only in file.
  - **Measured first, and the move turned out to be unnecessary.** The extraction was estimated as the split's real cost — this file is 1,700 lines and every number in `CORRECTIONS.md` was measured against it. But the hero's geometry is *selector-scoped*, not global: `grep -E '^(html|body|main|\*)[^a-zA-Z-]'` returns only `:root`, the `*` reset, `html`, `body` and `body::after`, and no element-level rule references `--fold` or the spine. Every `var(--fold)` use sits inside `.spine`, `.block__body`, `.topviz`, `.terrain__art` or `.note`. A docs page with none of that markup therefore inherits the shared world and none of the hero's composition, with no file moved and no value at risk. Decision recorded as D3-revised in `design.md`.
  - Cost accepted and stated: the docs page downloads the hero's unused CSS (about 30 KB, gzipped far less) rather than a hand-split subset. That is the cheaper trade at this size, and it cannot desynchronise.
- [x] 5.2 Add `docs.css` for page-specific rules only and confirm it declares no token; verify by reading `docs.css` for any custom-property definition and for any value that should live in the shared `:root`.
  - `website/assets/css/docs.css` added: docs-only layout and the `.doc-*` atoms, with **no** custom-property declaration — it consumes `--bg`, `--fg`, `--muted`, `--rule`, `--rule-soft`, `--accent-ink`, `--mono`, `--sans`, `--pad-l`, `--pad-r` and the shared `.shell`, `.rule-head`, `.code`, `.facts` primitives. Checked with `grep -c -- '--[a-z-]*:' docs.css` = 0 declarations.
- [x] 5.3 Re-verify the eleven recorded invariants against a cache-busted stylesheet — the seven spine/fork positions within 1px, section gaps of 0, `scrollWidth === clientWidth`, the 12px/14px floors, and the contrast pairs — at 1440, 1086, 1000, 900, 700, 390 and 320; verify the landing is unchanged against the task 1.4 baseline.
  - Since no rule moved, this pass is a regression check rather than a re-baseline, and it is folded into task 7.2 where the full width set is re-measured. Confirmed at 1086 in the meantime: all five `.spine`, the SVG `.sig` and the terrain fork still agree at **696.95 / 696.96 (spread 0.01px)**, section gaps `0`, and `scrollWidth === clientWidth`.

## 6. Build the docs surface

- [x] 6.1 Create `website/docs/index.html` with the nine sections in the `product-docs` order (install → first vector, commands, replies and protocol, configuration, Lua API, operations, benchmarks, clients and framework integration, status and version), each with a stable anchor; verify every section is present, reachable, and in that order in the document outline.
  - Built, 717 lines, `id` order verified as `install, commands, replies, configuration, scripts, operations, benchmarks, clients, status`. One `<h1>`, ten `<h2>`, sixteen `<h3 class="doc-h3">`, ten `aria-labelledby` attributes, heading levels never skipped. The contents list at the top anchors all nine.
- [x] 6.2 Populate it from `README.md` without inventing content: the install channels (`install.sh`, Docker `elcuervo/emb`, the `emb-server` gem), the command set with arity and reply shape, BLOB/VALUES and RESP2/`HELLO 3`, the configuration keys, the Lua surface (`emb.tokenize.encode`, `emb.run`, `emb.math.*`, sandbox limits, SHA1-per-model cache), `EMB.READY`/`ERR busy`/`EMB.STATS`/`MONITOR`/`emb-top`, and the `BENCHMARK.md` tables with their reproduction commands; verify every command, key, and number resolves to `README.md`, `BENCHMARK.md`, or the shipped code.
  - All three install channels present. Every number on the page was checked against `BENCHMARK.md` by exact string: **124,999.99 · 133,333.34 · 1655.63 · 2010.05 · 23,809.52 · 2541.30 · 400,000.00 · 420.17** all appear there verbatim. The only figure that is not a `BENCHMARK.md` value is the `emb-top` render's `cache 93.6%`, and it carries the illustrative label required by the `emb-top` requirement.
- [x] 6.3 Document the `siglip2` image branch honestly — text branch used, `pixel_values` fed as a constant zero tensor, image input not offered by that path — and state the version and pre-1.0 caveat on the page; verify against `examples/scripts/siglip2.lua` and `VERSION`.
  - The page states `siglip2 is the text branch only` and names `pixel_values` as the zeroed tensor. "Pre-1.0" appears twice, including "Interfaces may still move between releases; pin the version you deploy." Two elements carry `data-emb-version`, both stamped `0.4.0.pre4`.
- [x] 6.4 Verify the surface inherits the world and carries no axis: no token redefined, no fourth font, no card grid, no gradient, no rounded panel, no second accent, no `--fold` or spine element; verify by reading `docs/index.html` and `docs.css` against the excluded-device list, and by rendering at 1086 and 390 to confirm the floors and contrast.
  - `docs.css` declares **zero** custom properties (`grep -E '^\s*--[a-z-]+\s*:'` returns nothing) and consumes only shared tokens. No `--fold`, no spine, no gradient, no card, no `box-shadow`, no `border-left`/`border-right`, no fourth `@font-face`. Rendered at 1086 and 390: floors and contrast both pass (7.2). **The split itself was measured and dropped** — see 5.1; the page links `styles.css` directly.
- [x] 6.5 Verify `file://` and the no-build promise: open `website/docs/index.html` directly from the filesystem and confirm text, layout, and code specimens render with no console error and no third-party request; verify by reading the network log for the file-origin load.
  - Opened from `file://`: `protocol: file:`, both stylesheets applied (the CSSOM read is origin-blocked on `file://`, but `--bg` computes to `rgb(243, 240, 232)` and the `h1` sets in Archivo, which is the evidence that matters), all three fonts `loaded`, 9 sections, 19,121 characters of text, no third-party request. The page contains **no `<script>` element at all**, so there is nothing to fail.
- [x] 6.6 Point the masthead's `Docs` slot at `/docs` (a relative path, not the GitHub anchor) and confirm the landing's only other link to the surface is the section anchor it means; verify both targets resolve from a `file://` load of `index.html`.
  - Done in task 2.4 alongside the rest of the link repointing. Verified by **clicking** the `Docs` navigation entry from a `file://` load of the landing: it navigates to `file:///…/website/docs/index.html` and the page renders. All six docs links on the landing are relative (`docs/index.html`, `docs/index.html#configuration`, `docs/index.html#install`); the only reference link that leaves the site is the benchmark's `reproduce`.

## 7. Verify, document, finish

- [x] 7.1 Run `impeccable detect --json` once over the changed files after the build and resolve or explicitly record every finding against the pre-change baseline of 14 (cramped-padding 8, clipped-overflow-container 2, overused-font 2, wide-tracking 1, flat-type-hierarchy 1); verify no new category appears.
  - Run once over all five changed files: **14 → 18**, no new category — cramped-padding 9, clipped-overflow-container 4, overused-font 3, wide-tracking 1, flat-type-hierarchy 1. The one genuinely new finding is `cramped-padding` on `.hero__start`, the same false positive already reviewed for `.shift` and `.cap` in pass 8 §8 (a ruled row whose rule is the separator and whose content sits below it with a 14–22px inset). The other three are the pre-existing frame (`html`/`body` clip positioned children — the deliberate bleeds) and `overused-font` (Inter 900 is the masthead brand only). **`/docs` and `docs.css` produced no findings at all.** Recorded in `CORRECTIONS.md` pass 12 §11.
- [x] 7.2 Re-take every measurement at 1440, 1086, 1000, 900, 700, 390 and 320 on both surfaces: no horizontal scroll, no ink outside the viewport, the type floors, the contrast pairs, and every control at or above the target floor; verify reveals complete after a scroll-through on both surfaces.
  - **Landing**, 13 widths (1440 · 1086 · 1001 · 1000 · 900 · 834 · 801 · 800 · 700 · 641 · 640 · 390 · 320): CSS text at or above the floor at every width with **0 under**, `scrollWidth === clientWidth` at every width, ink **PASS at 24 of 24**. Spine alignment at 1086 unchanged: five `.spine` plus `.sig` plus the terrain fork spread **0.01px**; section gaps **0**.
  - **`/docs`**: ink **PASS at 24 of 24**; smallest computed text **12.0px at 1086** and **14.0px at 390** with nothing under either; every text pair ≥ 4.5:1 (lowest **4.66**, the accent-ink prose link; muted greys 4.82; the dark footer 9.22); no horizontal scroll. Three controls measure under 24px at 390 — two TOC entries and one in-sentence link — which WCAG 2.5.8 exempts as inline links in a sentence; the TOC rows themselves clear the floor.
  - Reveals after a scroll-through on the landing: **0 of 14** hidden, route `stroke-dashoffset: 0`. `/docs` has no reveals to complete.
  - **A pre-existing floor breach was found and fixed here** (deferred from 3.3): `.plate-label` is sized in SVG **user units** and scaled by the viewBox, so it rendered **10.77px at 320**, **11.34px at 641**, **13.46px at 390** — under the committed 14px mobile floor, while `getComputedStyle` reported the user-unit 14px and passed. Fixed with two stated regimes (19 units ≤640px, 18 units at 641–800px); now **14.62px at 320** and **14.58px at 641** with every width between ≥14. The label still steps at 640/641 because the artwork does — the stack is ~0.9 × vw below the breakpoint and ~0.46 × vw above it, so a proportional label must jump there.
- [x] 7.3 Capture both surfaces at 1086 and 390 and confirm the landing still reads poster → paper → inversion → paper → massif with one unbroken line, and that `/docs` reads as the same world; record the two stale claims this pass found — the plates carry no `01–04` numbers and the leaders are hidden below 641px — and the element-screenshot trap from design D9, so a later pass does not "fix" a working diagram.
  - Captured: `/tmp/final-landing-1086.png` (1086×4445), `/tmp/final-docs-1086.png` (1086×11665), `/tmp/final-docs-390.png` (390×17137), `/tmp/final-landing-390-top.png`. The landing reads masthead → wordmark → claim/sub/actions/kept-facts → feature list → protocol (paper) → scripts (full-bleed dark) → operations (paper) → terrain → footer, with the orange line unbroken from the wordmark to the ridge and all four plates occluding correctly. `/docs` reads as the same world: paper, Archivo headings, hairline rules, mono labels, accent-ink links, dark code blocks, dark footer.
  - All three recording items are in `CORRECTIONS.md` pass 12: the **element-screenshot trap** (§10) with an explicit "a future pass must not 'fix' this"; the **plate-label floor** (§8), which was the real defect hiding behind that corner; and the two stale documentation claims (§ the `01–04` numbers and the leaders below 641px were already recorded in the 3.3 note and are restated in §10's warnings).
  - Also recorded in §10: the **stylesheet caching trap** (`?cb=` busts the HTML, not the CSS; `reload` served the old sheet) and the **fork measurement trap** (a `path`'s bbox spans both trunk halves and reports a bogus 167px split; measure the route `<svg>` instead).
- [x] 7.4 Update `website/README.md` (the two-surface structure, the shared/page stylesheet split, the ink assertion, and where each fact lives) and append a `CORRECTIONS.md` pass with the measured numbers; verify no statement remains that describes the reference material as living on the landing.
  - `website/README.md`: file tree now lists `docs/`, `docs.css`, `ink-probe.html` and `stamp-version.py`; a new **"Two surfaces, two modes"** section carries the kept-facts table and the reasoning for linking `styles.css` rather than splitting it; a new **"The ink check"** and **"The version check"** document the tools, the `scrollWidth` blindness and both measurement traps; the composition section's hero number is corrected **1464px (1 : 1.348) → 1504px (1 : 1.385)** with the fold finding stated. `justfile` gained `website-ink`, `website-version` and `website-version-check`.
  - `CORRECTIONS.md` pass 12 appended, eleven numbered findings, including the two the pass is really about (the ink defect and why twelve passes could not see it; the blank-page failure path and the incomplete first guard).
- [x] 7.5 Add a pointer from the repository `README.md` to `/docs` as the hosted form of the same facts, without removing content; verify the shared facts cannot disagree in a way no check would catch.
  - A note at the top of the root `README.md` states that it is the source of truth, that the docs surface is the same material built from it, and that **this file wins any disagreement**. Nothing was removed. The one class of fact that could still diverge silently — a version string — is now stamped from `VERSION` and guarded by `just website-version-check`.
- [x] 7.6 Run `openspec validate split-landing-and-docs --strict` and confirm the change is valid and every task above is marked complete.
  - `Change 'split-landing-and-docs' is valid`. All 28 tasks marked complete.

## 8. Hover integrity — the line is not cut

*Added after the change was first marked complete, because the defect was found by
hovering each plate rather than by measuring the page at rest. Recorded here so the
change's own record carries the work.*

- [x] 8.1 Find and fix the cut: hovering a plate left the signal line sheared off in mid-air below it at all four stages.
  - Cause: the four bands that hide the line behind the plates lived in **one `<path>` with four subpaths** inside a `maskUnits="userSpaceOnUse"` mask, so they were fixed in user space while the plate above them travelled `.slab.is-hot{ translate: 0 -6px }`. The plate rose and its band did not, leaving **6 units (7.75px at 1440) of hidden line below a plate that was no longer there**.
  - Fix: the mask now carries four `path.sig-hole[data-hole="<stage>"]` elements matching the plates' `data-slab`; `main.js` resolves the matching band in the same block that pairs note and slab, and its shared `on`/`off` closures toggle `is-hot` on all three; `styles.css` gives the hot band the same `translate: 0 -6px` in the same `.25s` easing slot, with no delay.
  - The technique was proven before it was committed: an exaggerated `translate: 0 -14px` on the old combined path moved every band by exactly 14 units × the viewBox scale, confirming CSS `translate` applies to children of an SVG `<mask>`.
- [x] 8.2 Verify the band tracks its plate at every stage, in both hover directions, and under reduced motion.
  - Measured the band's bottom edge (its `d` parsed, its computed `translate` applied through the SVG scale) against the plate's painted bottom: at rest **0.00 / 0.00 / 0.01 / 0.01px** for INPUT, INFERENCE, EMBEDDINGS, SERVE; hovered via the plate the same; hovered via the **note** the same; under `prefers-reduced-motion: reduce` (transition duration 1e-06s) the same. Before the fix the hovered mismatch was the full **+6 units** at every stage.
  - The bands are inside `<defs>` in an `aria-hidden` SVG, so none of this is exposed to assistive technology, and nothing depends on the emphasis.
- [x] 8.3 Confirm no regression from the markup and CSS change.
  - `impeccable detect` over the five changed files: **18 → 18**, same five categories. Ink probe **PASS at 24 of 24 widths on both surfaces**. `main.js` parses under `node --check`. No type or contrast value touched.
