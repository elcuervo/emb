## 1. Verification spike

- [x] 1.1 Confirm the model pair: start a local server with the embedding model and the classifier, and verify `EMB <classifier> "text"`, `EMB <classifier> VALUES "text"`, and `EMB.MULTI` across both return the expected dimensions (384 and the classifier's label count) via `redis-cli`; record the failure if the classifier does not auto-configure with `pooling: none`, `normalize: false`, `dim` set explicitly
  - Pair: embedding `Xenova/all-MiniLM-L6-v2` at `models/minilm/`, classifier `Xenova/distilbert-base-uncased-finetuned-sst-2-english` at `models/sst2/`. The classifier **did** auto-configure — no explicit `pooling`/`normalize`/`dim` block is needed: the log at `127.0.0.1:16399` reads `sst2: detected dim=2 from ONNX graph`, `sst2: auto-detected output="logits" rank=2`, `sst2: auto-detected pooling=none`. `normalize: false` is the unset default and the raw reply confirms it (logits `-4.331916` / `4.709412`, not unit-norm). Measured via `redis-cli`: `EMB minilm "hello world"` → 1536 B (384 float32s); `EMB sst2 "this film is great"` → 8 B (2 float32s); `EMB minilm VALUES` → `shape 1 384`; `EMB sst2 VALUES` → `dtype FLOAT` / `shape 1 2` / `-4.331916332244873`, `4.709412097930908`; `EMB.MULTI minilm "hello" sst2 "this film is great"` → 1544 B (384×4 + 2×4 in one reply); `EMB.MODELS` → `minilm 384 ready`, `sst2 2 ready`; `EMB.READY` → `OK`; the preloaded preset replies `label POSITIVE` / `confidence 0.99988`. No failure to record.
- [x] 1.2 Record the measured RSS of the pair after load, and use it to size the machine rather than guessing
  - Measured steady RSS **1 467 040 KB ≈ 1.40 GiB** (`ps -o rss= -p <pid>`, three readings one second apart, after warming both models with `EMB` and `EMB.MULTI`), fp32 weights (`model.onnx` 90 MiB + 268 MiB). That sets the floor for 5.2: a 2 GB machine leaves ~0.6 GiB of headroom, so the deployment should either take 4 GB or switch the config to quantized/pre-quantized exports and re-measure.

## 2. RESP3 decoding in the shared client

- [x] 2.1 Add the RESP3 reply kinds the server emits (`%` map, `,` double, `_` null) to `internal/resp`, so a decoded reply preserves kind and ordering; verify with a table-driven unit test covering nested maps, typed doubles, and nulls
  - `Reply` gains `Float float64` for `,` and treats `_` as `Type: '_', Nil: true`; `%` decodes into `Elems` as alternating key/value (wire order preserved) with a `MapPairs() [][2]Reply` accessor. Tests `TestReadReplyRESP3Shapes` (map with a nested array, a `,0.5` double, and a `_` null), `TestReadReplyRESP3NestedMap`, and `TestReadReplyRESP3NullIsNotAnEmptyBulk` (a `_` and a `$0` must stay distinguishable) pass under `CGO_ENABLED=0 go test ./internal/resp/...`.
- [x] 2.2 Add protocol negotiation to `internal/resp` (send `HELLO 2`/`HELLO 3` and keep the negotiated version on the connection); verify the same command decodes to a flat array under RESP2 and a map under RESP3 in one test
  - `Client` tracks `proto` (reset by `Dial`); `Proto()` reports 2 until `Hello(3)` succeeds; `Hello(version)` sends `HELLO 2|3`, reads the reply (itself an array under RESP2 and a map under RESP3), maps an error reply to a Go error, and is a no-op when the connection already speaks the version. `TestHelloNegotiatesProtocol` drives HELLO 3 (map) → HELLO 2 (flat array) → a flat-array reply on the same connection; `TestHelloRejectsUnknownVersion` covers version 4 and a disconnected client.
- [x] 2.3 Run `just verify-harness` and confirm `emb-top` and the verifiers are unchanged
  - `just verify-harness` → all four packages ok (`internal/resp`, `internal/embverify`, `cmd/emb-verify`, `cmd/emb-multi-verify`). No caller changed: the added `Reply` field and `Type` values are additive, and `WriteArgv`'s encoding is untouched.

## 3. Sandbox bridge service

- [x] 3.1 Create `website/repl/` in the root module with an HTTP server exposing `GET /api/health` and `GET /api/ready`; verify `go test ./website/repl/` passes and `ready` is false until the server answers
- Created `website/repl/` in the root module: `main.go` (flags, preset manifest), `bridge.go` (HTTP surface, upstream round trip, envelopes), `allowlist.go`, `envelope.go`, `limits.go`. `GET /api/health` answers `{"status":"ok"}`; `GET /api/ready` answers `ready:false,state:"starting"` while nothing listens and `ready:true,state:"ready"` once a server does. Verified by `TestHealthReportsOK` and `TestReadyFalseUntilServerAnswers` under `CGO_ENABLED=0 go test ./website/repl/`.
- [x] 3.2 Implement the upstream client: one serialized connection, negotiation toggled per request, connect-on-demand warmup, and reconnect after loss; verify with a test against a stub listener that a request before the listener exists reports "starting" and succeeds after
- One serialized connection (`Bridge.mu`), `HELLO 2|3` sent on that connection per request (`Bridge.send`), connect-on-demand (`resp.Client.EnsureConn`), and one retry after a dropped connection (safe: the whole surface is read-only). Verified by `TestExecReportsStartingThenSucceeds` (no listener yet → `code:"starting"` at 503; listener up → the real reply), `TestExecReconnectsAfterLoss` (the peer drops the socket; the next command still answers), and `TestProtocolIsNegotiatedPerRequest` (HELLO 3 then HELLO 2 on one connection).
- [x] 3.3 Implement whole-argv validation and the fixed allowlist, with each refusal mapped to a legible error reply; verify a table-driven test that every denied command and every widened permitted command is refused and names the surface
- `validateArgv` checks the whole argv: the fixed allowlist, the exact shape of every permitted command (rejecting a widened `EMB.MODELS`, `EMB.INFO`, `PING`, `HELLO`, `EMB`, `EMB.MULTI`, `EMB.EVSHA`), and a named reason for each refused family (`CONFIG`, `AUTH`, `MONITOR`, `SHUTDOWN`, `EMB.SAVE`, `EMB.CACHE.FLUSH`, `EMB.SCRIPT`, `EMB.EVAL`, `EMB.IMG*`). Every refusal names the permitted surface. Verified by `TestValidateArgvRefusesOutsideSurface` (24 cases, each asserting the surface text), `TestValidateArgvPermitsSurface`, and `TestExecuteRefusalNeverReachesTheServer` (the fake server received nothing).
- [x] 3.4 Implement the preset manifest: load the preset digests the server preloaded, accept `EMB.EVSHA` only for those digests, and reject any other; verify a test that an unknown digest is refused and a known one is forwarded
- `loadPresets` reads the server's own config and hashes each listed script's bytes, so the manifest is the digest of the loaded source (`sha1(bytes)`, matching `server.scriptSHA`). `EMB.EVSHA` is accepted only for a `(model, sha)` in it; `GET /api/presets` publishes the list so a client never transcribes a digest. Verified by `TestPresetManifestForwardsKnownDigestAndRefusesOthers` (unknown digest and a digest for the wrong model both refused, and the server never saw them), `TestLoadPresetsMatchesTheServerRule`, and `TestPresetsEndpointPublishesTheManifest`.
- [x] 3.5 Implement the reply envelope (`kind`, `text`, `b64`, `float`, `int`, `elems`, `vector`) including the binary embedding descriptor; verify a test asserting a BLOB reply round-trips byte-exact through `b64` and reports the element count
- `Envelope` discriminates every kind (`bulk`, `status`, `int`, `double`, `nil`, `error`, `array`, `map`), keeps map order as alternating key/value `elems`, and carries `code` on bridge-produced errors. A BLOB embedding is `kind:"bulk"` with `b64` (never text) plus `vector:{dtype:"float32",count:384}`. Verified by `TestEnvelopeBlobRoundTripsByteExact` (bytes compared after base64 decode), `TestEnvelopePreservesKindsAndOrder` (map with a typed double and a nested array; the RESP2 VALUES array keeps its kinds), and `TestEnvelopeErrorIsNotAValue`.
- [x] 3.6 Implement the spend bounds: per-client and global token buckets, a concurrency semaphore, request and text caps, and a rolling work ceiling; verify tests that each bound refuses independently and that the ceiling refusal is distinct from the rate refusal
- `limits.go` holds a per-client token bucket, a global token bucket, a bounded concurrency semaphore, request/text caps, and a rolling-window work ceiling. Verified by `TestRateBoundRefusesIndependently`, `TestGlobalRateBoundRefusesIndependently`, `TestConcurrencyBoundRefusesIndependently`, `TestRequestAndTextCapsRefuseIndependently` (argv, text count, and byte caps each refuse with their own message), `TestWorkCeilingIsDistinctFromRate` (the ceiling says "at capacity", the rate names the rate limit), and `TestExecuteAppliesBounds` (HTTP 429 with `code:"capacity"`).
- [x] 3.7 Implement the error mapping so transport failure, not-ready, capacity, and timeout are four distinguishable replies; verify a test per case
- Four distinguishable replies, each with its own code and HTTP status: transport failure (`unavailable`/502), not ready (`starting`/503), capacity (`capacity`/429), timeout (`timeout`/504). Verified by `TestErrorMappingDistinguishesFourConditions`, one subtest per case (the timeout case drives a silent listener).
- [x] 3.8 Set the CORS allowlist for the site and its preview aliases; verify a preflight from a disallowed origin is refused and from the allowed origin is not
- A fixed origin allowlist (`-origins`), plus the request's own origin so the sandbox's own page — which the browser sends `Origin` for even same-origin — is never refused (the header is derived from `X-Forwarded-Proto`/`Host` behind the proxy). Verified by `TestCORSAllowlist` (a preflight from an unknown origin is 403; from `https://emb.is` it is 204 with the allow header), `TestCORSSameOriginIsAllowed`, and `TestCORSForwardedSchemeMatchesOrigin`.

## 4. Presets and server configuration

- [x] 4.1 Add canonical preset Lua files under `website/repl/presets/` for the embedding and classification demonstrations; verify each compiles by preloading it against a local server
- `website/repl/presets/embed.lua` (calls the server's own embedding path via `emb.embed`, returns dim/norm and an optional cosine similarity — a structured, non-embedding reply) and `website/repl/presets/classify.lua` (the sequence-classification recipe owned by the sandbox). Both were preloaded against a local server (`preloaded script ... for model "minilm"/"sst2"`) and called by digest: `embed` → `dim 384`, `norm 1.00000002…`, `similarity 0.6677…`; `classify` → `label POSITIVE`, `confidence 0.99988…`, `scores[…]`, and a wrong label count → `expected 2 labels, got 1`.
- [x] 4.2 Add the sandbox server configuration naming the two models on `/data/models/...`, binding loopback, setting the request caps, and listing the preset scripts; verify a local run serves `EMB.READY` `OK` and `EMB.MODELS` lists both
- `website/repl/sandbox.yaml` binds `127.0.0.1:6379`, caps `max_texts`/`max_pairs`/`max_command_bytes`, names both models on `/data/models/...` (so `downloadModel` writes onto the mounted volume), sets `normalize: true` on the embedding model, and lists `presets/*.lua`. Verified by a local run of the same config with local model paths: `EMB.READY` → `OK`, `EMB.MODELS` → `minilm 384 ready`, `sst2 2 ready`. The classifier auto-configured (`dim=2`, `pooling=none`, `output="logits"`), so no explicit block was needed; `normalize: true` on minilm was added because the measured norm without it was 5.73, which would have made the preset's documented norm of 1 wrong.
- [x] 4.3 Generate the preset digest manifest from the same files and add a check that the digest the site displays equals the digest in the manifest; verify the check fails when a preset byte changes
- `website/tools/stamp-presets.py` derives each digest from `website/repl/presets/*.lua` and writes it into `data-emb-preset-*` on the console section; `--check` fails on drift and also asserts `sandbox.yaml` preloads each preset. `just website-presets` / `website-presets-check` wrap it, and both `ci.yml` and `site.yml` run the check. Verified: appending a byte to `classify.lua` made `--check` fail naming `51ae48b3… -> 322364ef…`; restoring the file returned `stamp-presets: ok`.

## 5. Fly deployment

- [x] 5.1 Add an image build that produces both the server and the bridge, and an entrypoint script that starts the server, starts the bridge, forwards the stop signal, and waits; verify locally that a stop signal reaches the server and the bridge stops serving after the server exits
- `website/repl/Dockerfile` builds `emb` (CGo + ONNX, same ORT/libtokenizers pins as the root image) and `repl` (pure Go) in one stage and ships both plus the config and presets; `website/repl/run.sh` starts `emb`, starts the bridge, forwards TERM/INT to the server, and exits non-zero if either process dies. Verified locally by running the unit: it served a real `EMB minilm VALUES` reply; `kill -TERM` on the unit produced `shutting down (signal: terminated)... server stopped` in emb's log with both processes gone and the bridge no longer answering; `kill -KILL` on emb made the unit exit 1 with `run.sh: emb exited; ending the unit`.
- [ ] 5.2 Add the deployment configuration: one machine sized from 1.2, a mounted volume, the HTTP service on the bridge port, health checks on the bridge, and the machine kept running; verify a deploy starts and the volume path is writable
  - **Blocked in this environment:** `website/repl/fly.toml` is written (one machine sized from the 1.2 measurement, one volume mounted at `/data`, the HTTP service on the bridge port with health checks on `/api/ready` and `/api/health`, `auto_stop_machines = "off"`, `min_machines_running = 1`), but `flyctl` is not installed and no Fly credentials are available here, so "a deploy starts and the volume path is writable" and the tasks below it (5.3's external half, 5.4, and all of section 10) could not be run. They need `just sandbox-deploy` from a machine with an authenticated flyctl, then the checks named in each task.
- [ ] 5.3 Verify the server port is not reachable from outside the machine and that a request to the server's port is not answered by `emb`
- [ ] 5.4 Attach the sandbox host name, confirm TLS is issued, and smoke `POST /api/exec` against it

## 6. Live console

- [x] 6.1 Implement the terminal client module: a quote-aware command tokenizer, the request sender, the reply renderer for every envelope kind, and the idle/running/result/error/starting/offline states
- `website/repl/terminal.js` is the single client module: a quote-aware `tokenize` (single/double quotes, backslash escapes, unterminated-quote error), the `POST /api/exec {args, proto}` sender, `format` for every envelope kind (status/error/int/double/nil/bulk with the binary `\xNN` preview and vector descriptor/array/map), and the idle/running/result/error/starting/offline states with bounded `starting` retries and backoff. It is served at `/terminal.js` and loaded by both surfaces.
- [x] 6.2 Wire the landing console to the client through the documented seam, remove the transcript engine and its highlighter, and un-withhold the panel; verify a permitted command returns a real reply and a denied one returns the server-side refusal
- The landing console is `data-console="live"` (no `hidden`), driven through `window.embTerminal`; the transcript map, the pattern highlighter and the `window.embConsole.exec` stub are gone from `main.js`. Verified in a browser against a local bridge: `EMB minilm VALUES "hello world"` returned the real envelope, `CONFIG SET cache 1GB` returned the bridge's refusal naming the surface, and with the module unavailable the panel rendered `offline — the sandbox client could not be loaded` with the controls disabled rather than a transcript.
- [x] 6.3 Update the console copy and badge to describe a sandbox that may reset; verify no pricing, account, uptime, or support affordance is present
- The badge reads `SANDBOX · MAY RESET`; the note says a real `emb` process answers over RESP, that the sandbox may reset, and that it refuses configuration/state changes. No pricing, account, uptime, or support affordance appears anywhere in the section.
- [x] 6.4 Add the starting and offline states to the console styles using existing tokens; verify each renders with no empty or broken panel
- Added `.console__proto`, `.console__proto-select`, `.console__state` (and its retry button) using existing tokens only, plus a 44px/14px bump in the small-viewport media block. Both new states were rendered in the browser (starting during a retry window; offline with a working Retry that recovered without a page reload) with no empty or broken panel.
- [x] 6.5 Replace the transcript `<noscript>` with a static specimen and verify the section is complete with JavaScript unavailable
- The `<noscript>` now holds a 18-line `SPECIMEN · CAPTURED FROM THE SANDBOX` with the VALUES envelope, the BLOB line, and the classifier reply, labelled as a specimen. Verified: with the `js` class removed the form computes to `display:none` and the section is not hidden, so the static specimen is what remains.
- [x] 6.6 Verify keyboard-only operation, the live-region announcement, the target floor, the type floor, contrast on the dark plate, and reduced motion collapse, as the existing console requirements demand
- Verified in the browser: ArrowRight on the focused REDIS tab moved selection to SCRIPTS with the roving `tabindex` (keyboard-only operation); the live region is still armed after the idle paint; at 390px every console control measures ≥44px and the smallest type is 14px; on the dark plate the measured contrast is ink 16.59:1, dim 9.22:1, accent 6.06:1 (the documented values); with `prefers-reduced-motion: reduce` the reply arrived complete in one frame; `just website-ink` still reports PASS at all 24 widths.

## 7. Standalone terminal

- [x] 7.1 Serve `GET /` from the bridge as a standalone terminal using the same client module and the site's tokens; verify the same command and protocol render identically on both surfaces
- `GET /` serves the standalone terminal (embedded `index.html`) with the same module at `/terminal.js`; the scripts mode fetches `/api/presets` for the digest rather than transcribing one. Verified in a browser: `EMB minilm VALUES "hello world"` and `EMB.EVSHA sst2 51ae48b3… 1 "this film is great" NEGATIVE POSITIVE` rendered identically on the standalone terminal and on the landing console.
- [x] 7.2 Verify the landing page's reference to the client module resolves to a served file and is asserted by a check
- The landing page loads `https://cli.emb.is/terminal.js`; `TestLandingPageTerminalReferenceIsServed` reads `website/index.html`, extracts that reference, and asserts this bridge serves the same path with a JavaScript content type and a body defining `window.embTerminal`. `TestStandaloneTerminalIsServed` covers `/` and the 404 path.

## 8. Publish boundary and CI

- [x] 8.1 Add `website/repl/**` to `.assetsignore` and to the expected served set in `website/tools/published-tree.py`; verify `just website-published` passes
- `/repl/` was added to `website/.assetsignore`, and `published-tree.py` gained a `SERVICE_PREFIX` assertion that every on-disk file under `repl/` was excluded and that the directory exists. `just website-published` passes (12 served, 28 ignored, 14 sandbox service files); deleting the `/repl/` line makes the check fail with one `unexcluded:` line per service file.
- [x] 8.2 Classify `website/repl/**` as code in the CI gate so its Go tests run while a site-only change still skips the server and gem jobs; verify both branches locally or in a pull request
- `ci.yml`'s classifier now emits `sandbox` (any `website/repl/**` change, or anything outside `website/`, or an undeterminable diff) and a `sandbox` job runs `gofmt -l website/repl` plus `CGO_ENABLED=0 go test ./website/repl/`. Simulated the classifier's shell logic against representative diffs: a site-only change → `code=false sandbox=false` (server/gem jobs and the sandbox skipped, site checks still run); `website/repl/**.go`, a preset, or `fly.toml` → `sandbox=true`; a file outside `website/` → both true. `site.yml` and `ci.yml`'s site job also run `stamp-presets.py --check`.
- [x] 8.3 Add `just` targets to run, test, build, and deploy the sandbox, documented alongside the existing website targets
- Added `just sandbox-build`, `sandbox-test`, `sandbox-run`, `sandbox-image`, and `sandbox-deploy`, documented in a `── the sandbox (website/repl) ──` block beside the website targets. `just sandbox-test` passes.

## 9. Documentation and product truth

- [x] 9.1 Update `website/README.md` with the sandbox's boundary, the one-client rule, and the preset digest check
- `website/README.md` gains a `## The sandbox` section (the boundary and its check, the local loop, the one-client rule, the preset digest check, and the no-hosted-offering statement), the tree listing now shows `repl/`, the served/ignored table lists `repl/`, and the console design notes describe the live executor instead of the placeholder.
- [x] 9.2 Update `PRODUCT.md` / `website/PRODUCT.md` so the sandbox is stated as a resetting demonstration and the "no hosted offering" line still holds; verify no claim of a service, tier, or SLA is introduced
- There is no root `PRODUCT.md`; `website/PRODUCT.md` now carries a bullet stating the sandbox is a resetting demonstration with no account, pricing, tier, uptime commitment, or support path, and that it does not qualify the "no hosted or managed cloud offering exists" line.

## 10. End-to-end verification

- [ ] 10.1 From the deployed origin, run each preset, the raw and `VALUES` forms, both protocol versions, and a multi-model call; verify each reply's kind and numbers against a local reference run
- [ ] 10.2 Verify every denied command returns a legible refusal and that configuration and shared state are unchanged after the attempts
- [ ] 10.3 Inspect the browser payload and confirm no password, server address, or credential is present
- [ ] 10.4 Exercise the per-client limit and the work ceiling and confirm the refusal messages are the ones specified, with the console rendering them rather than an empty panel
- [ ] 10.5 Confirm the console's offline state by blocking the sandbox, and confirm recovery without a page reload once it returns
