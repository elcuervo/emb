# Tasks

- [x] Create proposal
- [x] Create spec
- [x] Replace `Dir.glob` with `OnnxRuntime.ffi_lib.first` in `gems/emb-server/bin/emb`
- [x] Test `emb` binary with the fix locally
- [x] Push test tag and validate in CI
- [x] Archive change
