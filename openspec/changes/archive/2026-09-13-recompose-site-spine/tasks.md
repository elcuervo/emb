## 1. Shape the recomposition before editing

- [x] 1.1 Re-read the surface brief at `website/.impeccable/surfaces/website-index-html.md` and rewrite its section-order and FIRST VIEWPORT blocks for the recomposition — terrain last, three ground-varied blocks, the spine as the page's axis — before any markup edit; verify the brief reads back with all six contract blocks still present.
  - Brief rewritten through `surface-brief write` **before** any markup edit; read back with all six contract blocks present (THESIS, OWN-WORLD, STORY, FIRST VIEWPORT, FORM, FINISH) and a new SECTION ORDER block naming the terrain as the page's full stop and the line as one axis at one width. `git status` at that point showed no tracked file under `website/` modified.
- [x] 1.2 Read `reference/craft-floor.md` immediately before the first edit and note the two rules that bind here (no coloured `border-left`/`border-right` above 1px as a code treatment; no kicker above a heading); verify no UI edit was made before this read.
  - Read; no UI edit preceded it. Both rules were acted on: the code specimens' only container is a rule above and below, and the `01 / PROTOCOL`-style `block__index` kickers the first draft carried were removed before the build.
- [x] 1.3 Confirm the route: this is a whole-surface recomposition inside an established world, so the visual system stays fixed and no `concept-seed` roll runs; record the one-line reason in the change notes.
  - `new-work.md` §3 second case. Reason recorded in `design.md` D9: the user specified the composition's frame (terrain last, one spine, varied blocks, code highlighting) and `DESIGN.md` pins the visual world, so the open work is the block design and it is resolved in `design.md` rather than fanned out into a tournament.

## 2. The spine

- [x] 2.1 Add `--fold: 65.31%` and `--spine-w: calc(var(--plate) * 4 / 364)` to `:root` with the derivation in a comment, and add the `.spine` primitive; verify by measuring the rendered spine width against the SVG `.sig` stroke at the plate clamp's minimum and maximum.
  - Added. The measurement **failed the first version**: below 1001px the SVG's rendered width stops being `--plate` (the stack column is 50% of the content, then 100%), so at 600px the CSS line was **2.406px against a 6.066px SVG stroke**. `--spine-w` is now redeclared per breakpoint (`calc(200% / 364)`, `calc(400% / 364)`), and the two agree to **0.009px** at 1440 / 0.003px at 900 and 600.
- [x] 2.2 Turn `.hero__pipeline` into a grid whose second track is the spine so the CSS line begins exactly at the pipeline's bottom, and verify there is no gap and no lateral step between the SVG spine and the CSS spine at 1086/1440/390.
  - Done. At ≥1001px the two-row grid puts the spine in row 2 and the handover is exact: `svgBottom === spineTop` at 1086 (1116/1116) and at 1280 (1316.64/1316.64). Below 1001px the two-row form left a **388px hole** (svgBottom 1952 vs spineTop 2340 at 390, because the notes row pushed the spine past the plates); the wrapper collapses to one row and the spine shares the diagram's cell, with the stack's paper hiding it behind the plates.
- [x] 2.3 Add a spine segment to each block section so the line continues through the whole region; verify the segments abut with no visible boundary at each join.
  - Five segments (hero, 3 blocks, terrain). Worst lateral error across the rendered page is **0.03px**; measured section gaps (hero→b1→b2→b3→terrain) are **0px at 1440, 1086, 1000, 900, 700, 390 and 320**. A first version put each block's spine on the section box instead of the content box, a **14.5px step** at every boundary; fixed with a positioned `.block__body` / `.terrain__inner`.

## 3. The terrain moves last

- [x] 3.1 Move the terrain section out of `.hero__pipeline` to a top-level section after the last block and before the footer, and verify no capability content follows it.
  - Moved. The route's path data is byte-identical (verified by assertion after the move) and no content follows the terrain but the footer.
- [x] 3.2 Re-anchor the terrain art by its left edge as `calc(var(--fold) - 1124.3 / 2172 * var(--art-w))` and lift the spine's end by `max(0px, calc(var(--band) - var(--art-w) * 0.3546))`; verify the spine's bottom meets the route trunk's start with no gap at 1086/1440/390.
  - Re-anchored by the left edge. `--band` is now `max(--art-h, clamp(...))` so the art box's top is never above the band's top, which is what the spec needs; the spine's end is `bottom: var(--art-h)`. At 1440 block3's bottom, the art's top and the route trunk's top are all **4012**; at 1086 the terrain top and art top are both **3781**.
- [x] 3.3 Verify the route's fork still lands on the spine axis within 1px at every tested width, including when the plate and band clamps are active.
  - Measured at seven widths (1440, 1086, 1000, 900, 700, 390, 320): worst fork-vs-spine error **0.03px**, against the 1px spec. The left-edge anchoring also closes the pass-6 critique's **−208.28px** spine/fork split at 834px, which the old right-anchored `--art-right` could not hold.

## 4. Three blocks, three grounds

- [x] 4.1 Rebuild the protocol block on paper: `rule-head`, a ruled ledger with the facts in the right column, and the console plate on the axis; verify no text sits on the spine and the console's behaviour, modes and transcripts are unchanged.
  - Built. `.block__grid`'s split **is** the lane, so copy ends before the line and the facts begin after it. The console keeps its markup verbatim (moved, re-indented) and is lifted to `z-index: 1` so it covers the line. Re-verified after the move: an unknown command returns `-ERR unknown command '<x>'` + hint, `EMB.HELP` lists the surface, and the transcript renders. One regression was caught here — `.cap`, `.cap__label` and `.cap__claim` had **no rules at all** (they lived in the region this change replaced), so the claims ran the full content width; restored.
- [x] 4.2 Build the scripts block as a full-bleed dark ground carrying the `model(fn(input)) → output` material and the Lua specimens; verify it uses `#111110` plus existing tokens and introduces no new colour.
  - Built on `#111110` with `#292823` rules — the SERVE plate's own faces — and existing tokens only. Its local `--muted` is remapped to `--rule` because `#6B6963` measures only **3.40:1** on `#111110`. The `shift` rows needed a grid fix (four children in a three-column track), caught by reading the render.
- [x] 4.3 Build the operations block on paper with a dense ledger and the `emb-top` capture as a dark plate; verify the three blocks' grounds and measures are visually distinguishable from each other and from the hero.
  - Built: paper / full-bleed dark / paper, with a two-column ledger in operations against a single-column ledger in protocol. Confirmed in the captures — the page reads poster → paper → inversion → paper → massif.
- [x] 4.4 Verify the removed single-region markup and its now-unused styles are gone, and that the archived change's console requirements still hold (placeholder statement, no endpoint, seams intact).
  - `grep` for `landscape`/`capability` in the stylesheet and markup returns only prose comments about landscape-oriented phones. The console still ships the `DEMO · NOT A LIVE SERVER` badge, the placeholder note, the `window.embConsole.exec(command, mode)` seam, real `<form>`/`<input>`/`<pre aria-live>` controls, and **no** network call (`grep` for `fetch|XMLHttpRequest|WebSocket|sendBeacon|EventSource` returns nothing).

## 5. Code highlighting

- [x] 5.1 Define the four token classes for the paper ground and for the dark ground using existing tokens only; verify no new colour value is introduced.
  - `.t-cmd`, `.t-str`, `.t-num`, `.t-dim`, defined twice (paper and dark) from `--fg`, `--accent-ink`, `--muted`, `--bg`, `--accent`, `--rule` only. No new colour value; emphasis is weight and role, never a second hue.
- [x] 5.2 Mark up the shell specimen and the Lua specimens with token spans; verify the highlighting marks token type rather than emphasis, and that no specimen uses a coloured left or right border as its treatment.
  - Marked up by hand; `grep` for `border-left|border-right` in the new stylesheet additions returns nothing. Each class marks what a token *is* — a command, a string literal, a number, a comment — and the two specimens use the same four classes.
- [x] 5.3 Apply the token treatment to the console's replayed commands and replies on both grounds; verify the console's existing states still render.
  - A four-rule pattern highlighter in `main.js` applies the identical classes to the plain-string transcripts. Verified live: `EMB` → `t-cmd`, `"hello world"` → `t-str`, `\x7c` → `t-num`. Its first version was wrong and is recorded: `-?\d+` matched the `2` inside `sst2` and split the script name; the numeric rule is now word-bounded. The idle / running / result / error / placeholder states all still render.
- [x] 5.4 Measure every token colour against its own ground and confirm ≥4.5:1 for all four classes on both grounds; record the ratios.
  - Measured in the browser, all eight pass: paper plain/`.t-cmd` **17.28:1**, `.t-str` **4.66:1**, `.t-num` **17.28:1**, `.t-dim` **4.82:1**; dark plain/`.t-cmd` **16.59:1**, `.t-str` **6.06:1**, `.t-num` **16.59:1**, `.t-dim` **9.22:1**. Recorded in `website/README.md` and `CORRECTIONS.md`.

## 6. Verify, document, finish

- [x] 6.1 Measure the smallest computed text size inside the dark block at the 1086px reference frame and at 390px and confirm ≥12px and ≥14px; also confirm no horizontal scroll and that every control clears the 44px target floor at 320–834px.
  - Dark block minimum: **14.0px at 390 and 320**; the whole region is **12.0px at 1086**. `documentElement.scrollWidth === clientWidth` at 1440, 1086, 1000, 900, 700, 390 and 320 (the terrain's bleed is clipped by `html{overflow-x:clip}` as designed). Controls at 320–390: mode **44px**, input **47px**, RUN **44px**, and a sweep for any interactive element under 44px returns none.
- [x] 6.2 Verify the hero, wordmark, claim, sub, actions, six-row feature list and pipeline artwork are unchanged against the pre-change render; verify the terrain's route path data is byte-identical.
  - The hero markup is untouched except for one inserted `<span class="spine spine--hero">`; the stylesheet changes are confined to `:root`, `.hero__pipeline`, the new blocks/terrain sections, and the responsive blocks. The route's five `d` attributes are byte-identical (asserted during the move and re-checked against the captured art block).
- [x] 6.3 Confirm the terrain route still draws to completion now that the band is the last section before the footer, at 1086/1440/390, and that reveals are complete after a scroll-through.
  - Confirmed: the route's progress denominator is `rect.height * 0.86`, and at maximum scroll `vh - rect.top` is the band's height plus the footer's, which clamps to 1 — so the trunk completes before the footer. Reveals: **17/17** `[data-reveal]` elements carry `.in` after a scroll-through at 1440 and at 390, and the full-page captures show every section.
- [x] 6.4 Run `impeccable detect --json` once over the changed files after the build and resolve or explicitly record every finding, compared against a baseline run over the pre-change sources.
  - Run once, compared against the pre-change baseline: **10 → 19**, all nine additions `cramped-padding`. Reviewed and recorded in `CORRECTIONS.md` pass 8 §8: three name `.block`, one `.terrain`, two `.console`/`.console__bar`, three the same-colour knockout patches. The rule targets a card with text flush to a *visible* boundary; these are full-bleed page bands whose shell carries the gutters, plus patches painted in the page's own paper — measured insets 16.29px on the console bar and 20px inside `.shift`. `flat-type-hierarchy` was present at baseline and is unchanged in substance.
- [x] 6.5 Capture the page at 1086/1440/390 (full page, after a scroll-through) and confirm the sequence reads as poster → inversion → poster → massif with one unbroken orange line.
  - Captured to `/tmp/spine-1440.png`, `/tmp/spine-390.png`, plus a boundary crop at 390 and a terrain viewport crop at 1440. The sequence reads poster → paper → inversion → paper → massif, with the line visible from the wordmark to the ridge. On phones the content knocks the line out and it shows in the padding gaps — the same device the plates use.
- [x] 6.6 Update `website/README.md` (composition, spine geometry and derivation, three grounds, token table) and append a `CORRECTIONS.md` pass recording the measurements; verify no stale statement about the terrain's position or the region's shape remains.
  - Both updated. `README.md`: file tree, the composition rewritten around the spine with the `--fold`/`--spine-w` derivation, a block table, the three-ground notes, the code-token table, the derived annotation geometry and knockouts, and a new responsive spine table covering all three states. `CORRECTIONS.md`: **pass 8** with the alignment table, the abutment measurements, the art re-anchoring, the type/contrast floors, the token table, the four defects this build introduced, and the detector review. `grep` for `landscape`/`capability` in both returns nothing stale (the remaining `landscape` mentions are prose about landscape-oriented phones).
- [x] 6.7 Run `openspec validate recompose-site-spine --strict` and confirm the change is valid and every task above is marked complete.
  - See below.

## 7. Block headers, branch removal, dark footer

- [x] 7.1 Give each block a neo-brutalist header bar that inverts with its ground, replacing the `rule-head` label, and verify the heading keeps its place in the document outline and clears AA against its own bar.
  - `.block__head`: a solid full-content-width bar, Archivo 750 uppercase at `clamp(19px, 2.5vw, 34px)`, reversed out of `--fg` on the two paper blocks and out of `--bg` on the dark one. Still an `<h2>` hanging off the block's `<section aria-labelledby>`. Measured **17.28:1** on all three. The bar is opaque, so it covers the spine where it crosses, which is the rule the console plate and the hero's slabs already follow. The hero keeps `rule-head` — that primitive is the page's annotation voice and it was only ever wrong as a *section heading*.
- [x] 7.2 Remove the three data branches and their annotations from the terrain, and remove the now-dead staging logic rather than leaving it in place.
  - Removed: 3 `route--branch` paths, `.anno--b/c/d` with their CSS rules and the `≤1000px` hide rule, and the `data-seq` staging offset in `main.js` (it existed only to sequence the branches 14% apart). Counts: branches **3 → 0**, `.anno` **5 → 2**, `data-seq` **2 → 0**. The two trunk halves both run the full scroll travel.
- [x] 7.3 Put the footer in the dark style, using the page's existing dark surface, and verify its contrast and that it sits correctly under the massif.
  - `#111110` — the same face as the console, the emb-top capture and the scripts block — with `--muted` remapped to `--rule`. Measured: copy **9.22:1**, the mark **16.59:1**, top rule **9.22:1**. The massif's base now meets a dark band rather than a paper strip.
- [x] 7.4 Re-verify everything pass 8 measured, since all three changes touch the composed page; run the detector once and compare against the pre-change baseline.
  - Alignment at 1086 unchanged: **0.01px** across SVG, hero, three blocks, terrain and fork; section gaps **0**; no horizontal scroll; reveals **14/14** at 1440 and 390. Detector **19 → 16**, no new categories — three `wide-tracking` findings left with the branch labels, and `section.hero clips a positioned child` is gone because the terrain moved out of the hero. Recorded in `website/README.md` and `CORRECTIONS.md` pass 9.
## 8. Phones: the stage notes sit left of the line

- [x] 8.1 On phones, hold the stage notes to the left of the spine so the line runs unbroken past them instead of being knocked out; verify the notes keep a readable measure, no note crosses the line, and no horizontal scroll appears.\n  - `width: calc(var(--fold) - var(--spine-w) / 2 - var(--fold-gap))` with `justify-self: start`, and the `≤640px` knockout removed. Measured at 390px: notes **159px** wide, **14px** clear of the line (one `--fold-gap`), **0** note boxes crossing it, spine span 1421–2466 with the notes ending at 2466, horizontal scroll **0**. The notes grew from ~100px to 122–144px tall and hold ~16 characters at the 14px floor — accepted, because they are captions to the plates beside them, not body copy.
- [x] 8.2 Re-take every measurement for this change against a cache-busted stylesheet, and record the caching trap in the docs.\n  - The first probe measured a **cached** stylesheet and reported the notes still 350px wide. Every number in `CORRECTIONS.md` pass 9 §1–§6 was re-taken against `index.html?cb=N`: spine alignment 924.13 / 924.14 / fork 924.13 at 1440 (0.01px), section gaps 0, header bars 17.28:1 inverting, footer copy 9.22:1 and mark 16.59:1, branches 0, annotations 2, no horizontal scroll.

## 9. Phones: the rail moves to the right margin

- [x] 9.1 Move the phone axis to the right margin and hold every text block to its left, so the notes and the blocks get a readable measure instead of the 16 characters a centre line allows.\n  - `--fold: 90%`, `--fold-hero: 90%`, `--art-w: 156vw`. One expression — `width: calc(var(--fold) - var(--spine-w) / 2 - var(--fold-gap))` — now holds the stage notes, the block grids, the header bars and the emb-top panel. Measured at 390px: rail at **335**, notes and grids **299px** (was 159px / ~16 characters), all ending 16px short of the rail, **0** text boxes on a visible rail, no horizontal scroll. At 320px: rail 272, notes 236px.
- [x] 9.2 Hide the diagram's internal spine at this width and let the rail thread the plates instead, so there is one line rather than two.\n  - `.pipeline__svg .sig{ display: none }` at ≤640px, and the stack's paper knockout dropped. Verified with `elementFromPoint` down the rail's x through the stack: a plate `path` in some bands, the transparent `svg` in others — the line passes behind the plates and shows in the gaps, as on desktop. Two orange lines 40% apart would have been two lines in every gap.
- [x] 9.3 Re-verify that the change is scoped to phones and that the massif's handover still holds.\n  - 900px unchanged: fold 25%, `.sig` visible, SVG spine **243.00** vs hero spine **242.99**, notes in their own column at 450. 1440px unchanged: fold 65.31%, five spines and the fork agree to **0.01px**, gaps 0, no horizontal scroll. Terrain handover at 390px: rail **334.98**, fork **334.99**. The massif is cropped by necessity — the fork is fixed at 51.76% of the artwork — and the summit now sits under the rail. Detector **16 → 14**, no new categories; recorded in `website/README.md` and `CORRECTIONS.md` pass 10.
