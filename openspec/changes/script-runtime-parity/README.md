# script-runtime-parity

Close the residual scripted-runtime defects: bound script sessions by the pool's real concurrency, share the image cache, open image resources lazily, guarantee no resource leaks, and set metal-parity performance budgets.
